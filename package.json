{
  "name": "student-discount",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "dev:test-store": "sh scripts/dev-test-store.sh",
    "dev:test-store:clean": "shopify app dev clean --config shopify.app.toml --store macfox-test-app.myshopify.com",
    "dev:local-preview": "node scripts/local-preview-server.mjs",
    "check": "shopify app config validate --config shopify.app.toml --json && shopify app build",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {},
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "tianchuang",
  "private": true
}
